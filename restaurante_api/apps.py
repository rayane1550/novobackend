from django.apps import AppConfig #importar a classe da aplicação do django


class RestauranteApiConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'restaurante_api' #nome da aplicação restaurante_api 
